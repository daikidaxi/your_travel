<% provide(:title, "#{@user.name}フォロー") %>
<% breadcrumb :follows,@user %>

<div class="container">
  <p class="offset-md-3 col-md-6 text-center title-common">フォロー</p> 
  <div class="row">
    <div class = "container">
    <%if @users.count==0%>
      <p class="text-center">まだフォローしているユーザーはいません</p>
    <%else%>
      <%= render 'user',users: @users%>
    <%end%>
    </div>
  </div>
</div>